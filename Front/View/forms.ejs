<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= estudiante ? 'Editar Estudiante' : 'Nuevo Estudiante' %></title>
</head>
<body>
  <h1><%= estudiante ? 'Editar Estudiante' : 'Nuevo Estudiante' %></h1>
  <form action="<%= estudiante ? '/editar/' + estudiante.id : '/crear' %>" method="POST">
    <label for="nombre">Nombre:</label>
    <input type="text" id="nombre" name="nombre" value="<%= estudiante ? estudiante.nombre : '' %>" required><br>
    <label for="edad">Edad:</label>
    <input type="number" id="edad" name="edad" value="<%= estudiante ? estudiante.edad : '' %>" required><br>
    <label for="carrera">Carrera:</label>
    <input type="text" id="carrera" name="carrera" value="<%= estudiante ? estudiante.carrera : '' %>" required><br>
    <button type="submit">Guardar</button>
  </form>
</body>
</html>
